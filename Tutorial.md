
cd my-project
npm init -y   # package.json
npm install -D typescript # Ts = dev dependency in package-lock.json
npx tsc --init # tsconfig.json
npx tsc --version # tsc version : 7.0.2

# TypeScript

## TypeScript 

TypeScript is a typed superset of JavaScript that compiles to plain JavaScript.